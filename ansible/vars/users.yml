---
#
users:
  sysadmin:
    - name: 'okfn'
      group: 'sysadmin'
      authorized_keys:
        - nigelb
    - name: 'nigelb'
      group: 'sysadmin'
      authorized_keys:
        - nigelb
    - name: 'pwalsh'
      group: 'sysadmin'
      authorized_keys:
        - paul
    - name: 'amercader'
      group: 'sysadmin'
      authorized_keys:
        - amercader
    - name: 'brook'
      group: 'sysadmin'
      authorized_keys:
        - brook
    - name: dfowler
      group: 'sysadmin'
      authorized_keys:
        - dfowler

  ckan_dev:
    - name: 'vitor'
      group: 'ckan_dev'
      authorized_keys:
        - vitor

  openspending_dev:
    - name: 'stefan'
      group: 'openspending_dev'
      authorized_keys:
        - stefan
    - name: 'holger'
      group: 'openspending_dev'
      authorized_keys:
        - holger

  labs_dev:
    - name: 'rowan.crawford'
      group: 'labs_dev'
      authorized_keys:
        - rowan.crawford
    - name: 'joost.cassee'
      group: 'labs_dev'
      authorized_keys:
        - joost.cassee
    - name: 'yohan.boniface'
      group: 'labs_dev'
      authorized_keys:
        - yohan.boniface
    - name: 'ross.jones'
      group: 'labs_dev'
      authorized_keys:
        - ross.jones
    - name: 'pudo'
      group: 'labs_dev'
      authorized_keys:
        - pudo
    - name: 'pierre.yves'
      group: 'labs_dev'
      authorized_keys:
        - pierre.yves
    - name: 'umbrich'
      group: 'labs_dev'
      authorized_keys:
        - umbrich
    - name: tonyh
      authorized_keys:
        - tonyh

  monitoring:
    - name: 'nagios'
      group: 'monitoring'
      authorized_keys:
        - nagios

  openspending:
    - name: akariv
      group: 'openspending'
      authorized_keys:
        - akariv
    - name: roll
      group: 'openspending'
      authorized_keys:
        - roll
    - name: lkravets
      group: 'openspending'
      authorized_keys:
        - lkravets
    - name: iborysyuk
      group: 'openspending'
      authorized_keys:
        - iborysyuk
    - name: 'vitor'
      group: 'openspending'
      authorized_keys:
        - vitor

  opentrials:
    - name: akariv
      group: 'opentrials'
      authorized_keys:
        - akariv
    - name: roll
      group: 'opentrials'
      authorized_keys:
        - roll
    - name: 'vitor'
      group: 'opentrials'
      authorized_keys:
        - vitor
