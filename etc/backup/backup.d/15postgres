if ! [ -z ${DB_DUMP_DIR} ] ; then
    OUTPUT=${DB_DUMP_DIR}/pgdump.sql

    ${MKDIR} -p ${DB_DUMP_DIR}
    ${RM} -f -- ${OUTPUT}{,.gz}
    /usr/bin/sudo -u postgres /usr/bin/pg_dumpall > ${OUTPUT}
    /bin/gzip ${OUTPUT}
else
    echo "ERROR: DB_DUMP_DIR not set, cannot dump postgres" 1>&2
fi
