server {
	listen   8080 default;
	server_name  localhost;

	access_log  /var/log/nginx/localhost.access.log;

	location / {
		root   /var/www/nginx-default;
		index  index.html index.htm;
	}

	if ( $host = opendatachallenge.org ) {
		rewrite ^(.*)$ http://www.opendatachallenge.org/$1 last; 
	}
}
