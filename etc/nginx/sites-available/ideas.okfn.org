upstream ideas.okfn.org_server {
	# server unix:/tmp/gunicorn.sock fail_timeout=0;
	# For a TCP configuration:
	server 127.0.0.1:8001 fail_timeout=0;
}

server {
	server_name  ideas.okfn.org *.ideas.okfn.org;

	access_log  /var/log/nginx/ideas.okfn.org.access.log;

	server_name_in_redirect  off;

	location /m/ {
		alias /home/okfn/var/srvc/ideas.okfn.org/src/osqa/forum/skins/;
		expires 30d;
		access_log off;
	}

	location / {
		## straight-forward proxy
		proxy_redirect off;
		proxy_set_header Host $host;
		proxy_set_header X-Real-IP $remote_addr;
		proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

		proxy_pass   http://ideas.okfn.org_server;
	}
}


